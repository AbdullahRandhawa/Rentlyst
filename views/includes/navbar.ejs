<!-- Mobile Drawer Backdrop -->
<div class="nav-backdrop" id="navBackdrop"></div>

<nav class="navbar sticky-top">
  <div class="container-fluid px-3 px-lg-4 d-flex align-items-center justify-content-between gap-3">

    <!-- Left Div: Brand -->
    <div class="d-flex align-items-center">
      <a class="navbar-brand d-flex align-items-center m-0" href="/" style="gap: 1px;">
        <img src="/logo-red-cropped.png" alt="R" style="height: 21px; width: auto;">
        <span class="brand-text m-0"
          style="font-family: 'Outfit', 'Montserrat', 'Arial Black', sans-serif; font-size: 1.8rem; font-weight: 800; letter-spacing: -0.5px; line-height: 1; display: flex; align-items: center;">
          <span style="color: var(--primary);">ENT</span><span style="color: var(--text-dark);">LYST</span>
        </span>
      </a>
    </div>

    <!-- Middle Div: Search Bar (Center, visible md+) -->
    <div class="d-none d-md-flex align-items-center mx-auto" style="flex: 0 1 350px; min-width: 70px;">
      <form class="search-bar w-100 m-0" role="search" method="GET" action="/explore/search">
        <input class="form-control search-input" type="search" placeholder="Search explore..." name="q" />
        <button class="btn search-btn" type="submit">
          <i class="fa-solid fa-magnifying-glass"></i>
        </button>
      </form>
    </div>

    <!-- Right Section: All Links + Auth + User Menu -->
    <div class="d-flex align-items-center gap-1 gap-md-3 ">

      <!-- Primary Links (Moved from left) -->
      <a class="nav-link nav-pill" href="/explore"><i class="fa-solid fa-compass fa-lg"></i><span
          class="d-none d-lg-inline ms-1">Explore</span></a>


      <!-- Auth Links -->
      <% if(currUser) { %>
        <a class="nav-link nav-pill d-none d-sm-flex align-items-center" href="/explore/new">
          <i class="fa-solid fa-plus fa-lg"></i>
          <span class="d-none d-lg-inline ms-1">Add Listing</span>
        </a>
        <a class="nav-link nav-pill" href="/messages" style="position:relative; align-items: center;"
          id="navMsgBtn">
          <i class="fa-regular fa-comment-dots fa-lg"></i>
          <span class="d-none d-lg-inline ms-1">Messages</span>
          <span id="navUnreadDot"
            style="display:none;position:absolute;top:2px;right:2px;width:9px;height:9px;background:#e63946;border-radius:50%;border:2px solid var(--bg-white);"></span>
        </a>
        <a class="nav-link nav-pill" href="/agent"><i class="fa-solid fa-robot fa-lg"></i><span
            class="d-none d-lg-inline ms-1">Ask Rentlyst</span></a>
        <% } %>

          <% if(!currUser) { %>
            <a class="nav-link nav-pill d-none d-md-flex" href="/signup"><i class="fa-solid fa-user-plus"></i><span
                class="d-none d-lg-inline ms-1">Sign up</span></a>
            <a class="nav-link nav-pill d-none d-md-flex" href="/login"><i
                class="fa-solid fa-right-to-bracket"></i><span class="d-none d-lg-inline ms-1">Log in</span></a>
            <% } %>

              <% if(currUser) { %>
                <!-- Desktop User Dropdown -->
                <div class="dropdown ms-1 d-none d-md-block">
                  <div class="user-menu" id="dropdownMenuButton1" data-bs-toggle="dropdown" aria-expanded="false">
                    <i class="fa-solid fa-bars"></i>
                    <% if(currProfile && currProfile.profileImg && currProfile.profileImg.url) { %>
                      <img src="<%= currProfile.profileImg.url %>" alt="Profile"
                        style="width:32px;height:32px;border-radius:50%;object-fit:cover;display:block;flex-shrink:0;">
                      <% } else { %>
                        <div
                          style="width:32px;height:32px;border-radius:50%;background-color:#222;color:#fff;display:flex;align-items:center;justify-content:center;font-weight:700;font-size:0.85rem;flex-shrink:0;">
                          <%= currUser.username ? currUser.username.charAt(0).toUpperCase() : 'U' %>
                        </div>
                        <% } %>
                  </div>
                  <ul class="dropdown-menu dropdown-menu-end shadow-sm border-0 mt-2"
                    aria-labelledby="dropdownMenuButton1">
                    <% if(currUser && currUser.role==='admin' ) { %>
                      <li><a class="dropdown-item fw-semibold" href="/admin"><i
                            class="fa-solid fa-shield-halved me-2"></i>Admin Dashboard</a></li>
                      <li>
                        <hr class="dropdown-divider">
                      </li>
                      <% } %>
                        <li><a class="dropdown-item fw-semibold" href="/profile"><i
                              class="fa-regular fa-user me-2"></i>My Profile</a></li>
                        <li>
                          <hr class="dropdown-divider">
                        </li>
                        <li><a class="dropdown-item" href="/logout"><i
                              class="fa-solid fa-arrow-right-from-bracket me-2"></i>Log out</a></li>
                  </ul>
                </div>

                <!-- Mobile Avatar Hamburger -->
                <div class="mobile-avatar-pill d-sm-none ms-1" id="mobileMenuBtn"
                  style="display:flex;align-items:center;gap:2px;">
                  <% if(typeof currProfile !=='undefined' && currProfile && currProfile.profileImg &&
                    currProfile.profileImg.url) { %>
                    <img src="<%= currProfile.profileImg.url %>" alt="Avatar"
                      style="width:25px;height:25px;border-radius:50%;object-fit:cover;">
                    <% } else { %>
                      <div
                        style="width:23px;height:23px;border-radius:50%;background-color:#222;color:#fff;display:flex;align-items:center;justify-content:center;font-weight:700;font-size:0.75rem;flex-shrink:0;">
                        <%= currUser.username ? currUser.username.charAt(0).toUpperCase() : 'U' %>
                      </div>
                      <% } %>
                        <i class="fa-solid fa-bars fa-xl"></i>
                </div>
                <% } else { %>
                  <!-- Mobile Login Hamburger -->
                  <button class="mobile-menu-btn d-sm-none ms-1" id="mobileMenuBtn" aria-label="Open menu">
                    <i class="fa-solid fa-bars"></i>
                  </button>
                  <% } %>

                    <!-- Dark Mode Toggle Desktop -->
                    <button class="dark-mode-toggle ms-1 d-none d-md-flex" aria-label="Toggle dark mode">
                      <i class="fa-solid fa-moon dark-mode-icon"></i>
                    </button>

    </div>

  </div>
</nav>

<!-- Mobile Drawer -->
<div class="mobile-drawer" id="mobileDrawer">
  <div class="drawer-header">
    <span class="fw-semibold" style="font-size:1.2Srem;">Menu</span>
    <button class="drawer-close" id="drawerClose" aria-label="Close">
      <i class="fa-solid fa-xmark"></i>
    </button>
  </div>

  <!-- Same pill search as desktop -->
  <div class="drawer-search">
    <form class="search-bar w-100" role="search" method="GET" action="/explore/search">
      <input class="form-control search-input" type="search" placeholder="Search explore..." name="q" />
      <button class="btn search-btn" type="submit">
        <i class="fa-solid fa-magnifying-glass"></i>
      </button>
    </form>
  </div>

  <div class="drawer-nav">
    <% if(currUser) { %>
      <!-- User Profile Header -->
      <a class="drawer-link" href="/profile" style="padding-top: 0.5rem; padding-bottom: 0.5rem;">
        <% if(typeof currProfile !=='undefined' && currProfile && currProfile.profileImg && currProfile.profileImg.url)
          { %>
          <img src="<%= currProfile.profileImg.url %>" alt="Avatar"
            style="width:28px;height:28px;border-radius:50%;object-fit:cover;flex-shrink:0;">
          <% } else { %>
            <div
              style="width:20px;height:20px;border-radius:50%;background-color:#222;color:#fff;display:flex;align-items:center;justify-content:center;font-weight:700;font-size:0.75rem;flex-shrink:0;">
              <%= currUser.username ? currUser.username.charAt(0).toUpperCase() : 'U' %>
            </div>
            <% } %>
              <span class="fw-bold" style="font-size: 1.05rem;">My Profile</span>
      </a>
      <div class="drawer-divider"></div>
      <% } %>

        <!-- Main Actions -->
        <a class="drawer-link" href="/explore"><i class="fa-solid fa-compass"></i><span>Explore</span></a>
        <a class="drawer-link" href="/explore/new"><i class="fa-solid fa-plus"></i><span>Add New Listing</span></a>

        <% if(currUser) { %>
          <a class="drawer-link" href="/messages" style="position:relative;" id="drawerMsgBtn">
            <i class="fa-regular fa-comment-dots fa-lg"></i>
            <span>Messages</span>
            <span id="drawerUnreadDot"
              style="display:none;position:absolute;top:50%;right:12px;transform:translateY(-50%);width:9px;height:9px;background:#e63946;border-radius:50%;"></span>
          </a>
          <a class="drawer-link" href="/agent"><i class="fa-solid fa-robot"></i><span>Ask Rentlyst</span></a>

          <div class="drawer-divider"></div>

          <% if(currUser.role==='admin' ) { %>
            <a class="drawer-link" href="/admin"><i class="fa-solid fa-shield-halved"></i><span>Admin
                Dashboard</span></a>
            <% } %>
              <a class="drawer-link drawer-link-danger" href="/logout"><i
                  class="fa-solid fa-right-from-bracket"></i><span>Log out</span></a>
              <% } else { %>
                <div class="drawer-divider"></div>
                <a class="drawer-link" href="/signup"><i class="fa-solid fa-user-plus"></i><span>Sign up</span></a>
                <a class="drawer-link drawer-link-primary" href="/login"><i
                    class="fa-solid fa-right-to-bracket"></i><span>Log in</span></a>
                <% } %>

                  <div class="drawer-divider"></div>
                  <!-- Settings -->
                  <a class="drawer-link dark-mode-toggle" href="#" onclick="event.preventDefault();"><i
                      class="fa-solid fa-moon dark-mode-icon"></i><span>Toggle Theme</span></a>
  </div>
</div>

<script>
  (function () {
    const btn = document.getElementById('mobileMenuBtn');
    const drawer = document.getElementById('mobileDrawer');
    const backdrop = document.getElementById('navBackdrop');
    const closeBtn = document.getElementById('drawerClose');
    function openDrawer() { drawer.classList.add('open'); backdrop.classList.add('visible'); document.body.style.overflow = 'hidden'; }
    function closeDrawer() { drawer.classList.remove('open'); backdrop.classList.remove('visible'); document.body.style.overflow = ''; }
    if (btn) btn.addEventListener('click', openDrawer);
    if (closeBtn) closeBtn.addEventListener('click', closeDrawer);
    if (backdrop) backdrop.addEventListener('click', closeDrawer);

    // Apply active class to the current page link so it stays active
    const currentPath = window.location.pathname;
    const navLinks = document.querySelectorAll('.nav-pill, .drawer-link');
    navLinks.forEach(link => {
      const href = link.getAttribute('href');
      if (href) {
        if (currentPath === href) {
          link.classList.add('active');
        } else if (href === '/explore' && currentPath.startsWith('/explore') && !currentPath.includes('/new') && !currentPath.includes('/search')) {
          link.classList.add('active');
        }
      }
    });
  })();
</script>

<% if (currUser) { %>
  <script>
    (function () {
      const navDot = document.getElementById('navUnreadDot');
      const drawerDot = document.getElementById('drawerUnreadDot');

      function showDots(visible) {
        if (navDot) navDot.style.display = visible ? 'block' : 'none';
        if (drawerDot) drawerDot.style.display = visible ? 'block' : 'none';
      }

      async function checkUnread() {
        try {
          const res = await fetch('/profile/unread-check');
          const data = await res.json();
          showDots(data.hasUnread === true);
        } catch (e) {
          // Silently fail - don't break the page if this fails
        }
      }

      // Check immediately on page load, then every 15 seconds
      checkUnread();
      setInterval(checkUnread, 15000);
    })();
  </script>
  <% } %>